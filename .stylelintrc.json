{
  "extends": ["stylelint-config-standard"],
  "rules": {
    "at-rule-no-unknown": [
      true,
      {
        "ignoreAtRules": ["tailwind", "apply", "layer", "config", "theme", "import", "source"]
      }
    ],
    "import-notation": null,
    "no-descending-specificity": null
  }
}
